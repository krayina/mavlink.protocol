public byte[] Serialize()
{
    var buffer = new byte[1];
    buffer[0] = Index;
    if (Bank.HasValue)
    {
        Array.Resize(ref buffer, buffer.Length + 1);
        buffer[buffer.Length - 1] = Bank.Value;
    }

    return buffer;
}